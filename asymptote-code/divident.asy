import graph;
import geometry;
import gsl;

usepackage("amsmath");
usepackage("mtpro2");

settings.outformat="pdf"; // output is pdf

size(9cm, keepAspect=true);
defaultpen(fontsize(16pt));

label("$\displaystyle\frac{\text{divident}}{\text{divisor}}=\text{quotient}$",(0,0));